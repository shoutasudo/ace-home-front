/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'scdn.line-apps.com',
            'scontent-itm1-1.cdninstagram.com'
        ],
    },
};

export default nextConfig;
